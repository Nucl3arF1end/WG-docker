## First startup

1. Set environment
    cp env_example .env
    change variables in .env

2. Start docker container
    docker compose up -d

### Description

- https://hub.docker.com/r/linuxserver/wireguard
